10px solid status circle used next to section headers ("Vault mounted", repo group headers).

```jsx
const { StatusDot } = window.WorktreeManager;
<StatusDot status="success" /> <StatusDot status="sync" />
```
